import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['example.com', 'another-domain.com'], // Add domains for RSS feed images
  },
};
  /* config options here */


export default nextConfig;
